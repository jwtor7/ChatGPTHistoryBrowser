# No renderer capabilities

The application renderer communicates only with the authenticated loopback API. It receives no
Tauri command, filesystem, shell, dialog, opener, or network plugin capabilities.
